AF_PATH = /home/mtmorgan/bin/arrayfire

CXX_STD = CXX11
PKG_CPPFLAGS = -I${AF_PATH}/include -I${R_HOME}/include
PKG_LIBS= -L${AF_PATH}/lib -lafopencl -lOpenCL

OBJECTS = sparseMatrix.o kernelManager.o Source.o
